Issue :Lambda function timing out	Lambda executes but always times out before completion.	Scenario: Task timed out after X seconds; memory allocation insufficient; VPC configuration causing cold start delays; Symptoms: infinite loop in code.
